# Runtime data

This directory is reserved for local runtime state. Its future layout is `active.json`,
`slots/blue`, `slots/green`, and `downloads`. Runtime data and generated geospatial artifacts are
ignored by Git. M0 contains no dataset.
